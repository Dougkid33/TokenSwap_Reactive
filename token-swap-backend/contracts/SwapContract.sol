// contracts/SwapContract.sol
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

contract SwapContract{
    function swap(address token1, address token2, uint amount) public returns (bool){ 
        // Transfer token1 to contract
        // Transfer token2 to sender
        //Lógica de swap de tokens
        return true;
    }
}